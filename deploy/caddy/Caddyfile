{
	email {$ACME_EMAIL}
}

{$APP_DOMAIN:localhost} {
	encode zstd gzip

	handle_path /api/* {
		reverse_proxy signal-api:8765
	}

	handle /health {
		respond "ok" 200
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
